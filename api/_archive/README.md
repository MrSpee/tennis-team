# Archive - Nicht genutzte API Functions

Diese Functions werden nicht mehr verwendet und wurden hierher verschoben, um das Vercel Hobby Plan Limit von 12 Functions nicht zu überschreiten.

## Archivierte Functions

- `bulk-import-club-rosters.js` - Wird nicht mehr verwendet
- `create-player.js` - Wird nicht mehr verwendet

## Falls wieder benötigt

Diese Functions können wieder aktiviert werden, indem sie aus `api/_archive/` zurück nach `api/import/` verschoben werden.

